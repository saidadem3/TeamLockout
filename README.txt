To run the server cd into flask-backend folder and run "python app.py"
This will run on localhost:5000.

To run client cd into front-end folder and run "npm start"
This will run in localhost:3000

Back-end written in flask/python
Front-end written in react (using axios api)